

To run the project replace username and password with valid github credentials

```shell
    mvn test -Dspring.profiles.active=local -Duser-credentials.login=username -Duser-credentials.password=password -Ddevice.type=PC
```